char temp[] = "";
            // for (int i = strlen(iBinary) - 1; i > 0; i--)
            // {
            //     strncpy(temp, iBinary, i);
            //     printf("%s\n", temp);
            //     strcpy(temp, "");
            // }

            // printf("[Output:]\t2\n");